[tool.pytest.ini_options]
pythonpath = "."
addopts = "-p no:warnings"

[tool.ruff]
line-length = 88
exclude = ["alembic/"]

[tool.taskipy.tasks]
api = "uvicorn src.app:app --reload"
test = "pytest -v"
lint = "ruff check ."
format = "ruff format ."